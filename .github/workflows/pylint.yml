name: Pylint

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10"]
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pylint
    - name: Analysing the code with pylint
      run: |
        # Run pylint with exit-zero to prevent failure
        result=$(pylint --disable=E0401,W0611,W1309,C0103,C0301,E0611 --exit-zero $(git ls-files 'pipeline/*.py'))
        
        # Extract the score from pylint output
        score=$(echo "$result" | grep -oP 'Your code has been rated at \K[0-9]+')
        
        # Output the score for visibility
        echo "Pylint score: $score"
